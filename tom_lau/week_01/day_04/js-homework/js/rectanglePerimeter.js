// Part 1, Rectangle
//
// perimeter - Returns the perimeter of the rectangle


let rectangle = {
   length: 4,
   width: 4
 };

 let perimeter = function (rectangle) {
    let totalArea = rectangle.length + rectangle.width
      console.log (`Perimeter ${ rectangle.length } x ${ rectangle.width } = ${ totalArea }`);
    };
   perimeter(rectangle);
