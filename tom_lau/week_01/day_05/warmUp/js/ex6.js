// Part 2, Triangle
// area - Returns the area of the Triangle


let triangle = {
    sideA: 3,
    sideB: 4,
    sideC: 4
  };
  // 
  // let areaTriangle = function (triangle) {
  //   // debugger
  //   if (triangle.sideA !== triangle.sideB && triangle.sideC
  //      === triangle.sidec && triangle.sideB === triangle.sideC){
  //
  //     console.log (`This is a isosceles triangle.`);
  //   } else {
  //     console.log (`This is a  not isosceles triangle.`);
  //   }
  // };
  // areaTriangle(triangle);
  //
  //
  //
  // let area = function (rectangle) {
  //   let totalArea = rectangle.length * rectangle.width // this line casn be emebeded
  //     console.log (`Area of ${ rectangle.length } x ${ rectangle.width } = ${ totalArea }`);
  // };
  // area(rectangle);
  //
  //
  // for isObtuse:  a2+b2<c2, where a,b and c  are the sides of the triangle and c is the longest side
