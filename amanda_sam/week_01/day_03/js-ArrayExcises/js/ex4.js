/*
Define a function sumArray and a function multiplyArray that sums and multiplies (respectively) all the numbers in an array of numbers.
For example, sumArray([1,2,3,4]) should return 10,
and multiplyArray([1,2,3,4]) should return 24.
*/

const sumArray = function ( num1, num2, num3, num4 ) {
  console.log( num1 + num2 + num3 + num4 );
 }

 sumArray(1,2,3,4);

///////////////////////////////////////////////////////

 const multiArray = function ( num1, num2, num3, num4 ) {
   console.log( num1 * num2 * num3 * num4 );
  }

 multiArray(1,2,3,4);
